// A la hora de hacer bubles podemos hacerlos de arrays y de mas objetos 
// ejemplo con arrays
var frutas = ["manzana", "banana", "naranja", "uva"];
//tenemos el array 

for(var i=0;i<=frutas.length-1;i++){      
    //lo que estamos haciendo aca es crear un for que es un bucle que contiene una variable que es  " i " que es = 0
    // lo que decirmos que es i sea igual a una posicion que aumente y decimos que sea <= frutas.length que es pase por 
    //frutas en las posiciones 
    console.log(frutas[i])//aca imprimimos el for osea el buble que pasa por todos los objetos
}